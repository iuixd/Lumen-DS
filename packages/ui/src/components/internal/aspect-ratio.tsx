import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

/**
 * Straight re-export of Radix's AspectRatio — shadcn's own generated source
 * has no styling, icons, or colors to adapt here, so nothing changed.
 */
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
