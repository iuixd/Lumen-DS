---
"@lumen/ui": minor
---

Add `FilterChip` and `ChoiceChip`, two new Selection primitives sourced from the Buttons page (Lumen-AI-Design-System, nodes 581:409 and 581:485). Both are toggleable pills (`selected`/`disabled` props, `aria-pressed`/`aria-disabled`, `lg` size only). No new tokens required.
