---
"@lumen/tokens": patch
---

Rename the product from "Lumen Design System" to "Lumen AI Design System" throughout, matching the canonical Figma source file's own rename (same fileKey and node IDs — only the display name changed). Updates `_comment` citations in token source JSON (and their regenerated `dist/` output); no token values changed.
